/// Category identifier and display name (domain layer).
class CategoryDomain {
  const CategoryDomain({
    required this.id,
    required this.name,
  });

  final String id;

  final String name;
}

/// Copy for the main launch screen.
///
/// Import this library (or `package:auror/common/strings.dart`) and use the
/// top-level constants directly — no class prefix.
library;

const String badgePill = 'Microlearning com ciência';

const String heroHeadlineFirst = 'Aprenda em 7 minutos por dia';
const String heroHeadlineAccent = ' — e lembre de verdade.';

const String heroSubcopy =
    'Cards curtos + perguntas que te fazem lembrar + revisões no tempo certo. '
    'Para quem estuda muito e esquece rápido.';

const String featureSessions =
    'Sessões curtas (5–7 min) com foco no essencial';
const String featureRecall =
    'Active recall de verdade (você tenta lembrar)';
const String featureReviews =
    'Revisões automáticas (D+1, D+3, D+7)';

const String ctaEnterApp = 'Entrar no app';
const String ctaContinueStudying = 'Continuar estudando';
const String ctaHowItWorks = 'Ver como funciona ↓';

/// Dev-only entry to the design system catalog (non-production builds).
const String ctaDesignSystem = 'Design system';

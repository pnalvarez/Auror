/// Copy for the onboarding “real example” screen (interactive recall demo).
library;

const String onboardingRealExampleTitle = 'Exemplo real';

const String onboardingRealExampleSubtitle = 'Igual no app. Teste agora.';

const String recallTopicCard = 'Card';

const String recallQuestionActiveRecall = 'O que é \'active recall\'?';

const String recallInstructionMental =
    'Tente responder mentalmente antes de ver a resposta.';

const String recallExpectedAnswerBody =
    'Active recall é tentar trazer uma informação da memória (sem olhar o '
    'material) em vez de só reler. Isso fortalece a retenção e é mais eficaz '
    'do que revisar passivamente.';
